import { JsonRpcRequestPayload, IntermediaryEvents } from '@magic-sdk/types';
import type { SDKBase } from '@magic-sdk/provider/dist/types/core/sdk';
import type { ViewController } from '@magic-sdk/provider/dist/types/core/view-controller';
import type { EventsDefinition } from '@magic-sdk/provider/dist/types/util/events';
export declare class BaseModule {
    protected sdk: SDKBase;
    constructor(sdk: SDKBase);
    /**
     * The `ViewController` for the SDK instance registered to this module.
     */
    protected get overlay(): ViewController;
    /**
     * Emits promisified requests to the Magic `<iframe>` context.
     */
    protected request<ResultType = any, Events extends EventsDefinition = void>(payload: Partial<JsonRpcRequestPayload>): import("@magic-sdk/provider/dist/types/util/promise-tools").PromiEvent<ResultType, Events extends void ? {
        done: (result: ResultType) => void;
        error: (reason: any) => void;
        settled: () => void;
    } : Events & {
        done: (result: ResultType) => void;
        error: (reason: any) => void;
        settled: () => void;
    }>;
    protected createIntermediaryEvent<T = Function>(eventType: IntermediaryEvents, payloadId: string): T;
}
