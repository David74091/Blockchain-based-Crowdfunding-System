import { AxiosResponse } from "axios";
export declare type Paginated<T> = {
    items: Array<T>;
    nextToken: string;
};
export declare const isPaginated: (response: AxiosResponse) => boolean;
export declare const PAGINATION_HEADER = "next-token";
